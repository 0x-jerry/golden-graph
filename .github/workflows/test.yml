name: Test

on:
  push:
    tags:
      - "*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Setup bun
        uses: 0x-jerry/workflows/actions/setup-js@main

      - name: Install dependencies
        run: bun install

      - name: Run tests
        run: bun run test
